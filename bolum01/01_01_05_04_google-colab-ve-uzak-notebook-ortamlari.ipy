# ==========================================================================
# VERİ MADENCİLİĞİ VE MAKİNE ÖĞRENMESİ
# Python ile Temel Analitikten Büyük Veri ve Gerçek Zamanlı Sistemlere
# --------------------------------------------------------------------------
# Bölüm 1
# Konum : BÖLÜM 1: Python Ortamının Hazırlanması ve Temel Python Konuları › 1.1. Python Ortamının Kurulumu › 1.1.5. Geliştirme Ortami: IDE ve Kod Editörleri › 1.1.5.4. Google Colab ve Uzak Notebook Ortamları
# Dosya : bolum01/01_01_05_04_google-colab-ve-uzak-notebook-ortamlari.py
# Gerekli: pip install torch
# ==========================================================================
# Google Colab'da yerel dosyalara erisim
from google.colab import drive
drive.mount("/content/drive")

# Colab'da paket yükle
!pip install some-package

# Colab'da GPU varligini kontrol et
import torch
print("CUDA mevcut:", torch.cuda.is_available())
print("GPU:", torch.cuda.get_device_name(0) if torch.cuda.is_available() else "Yok")

# Colab'da sistem bilgisi
!nvidia-smi       # GPU bilgisi
!cat /proc/cpuinfo | grep "model name" | head -1
!free -h          # RAM
